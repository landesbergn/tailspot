node_modules
.next
out
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env*.local

# Store-ready PNGs. Regenerable via Export bundle, so kept out of history.
#   export/                       the current set (what you upload)
#   export-archive/<version>/     what was actually shipped for that version
export
export-archive
