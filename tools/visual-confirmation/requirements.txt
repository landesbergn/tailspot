# Tailspot visual-confirmation spike — pinned, working install.
#
# IMPORTANT environment constraint discovered on this machine:
#   This is an Apple Silicon Mac, but the only available Python interpreters are
#   x86_64 (Homebrew under /usr/local, run via Rosetta). There is NO native
#   arm64 Python here. PyTorch dropped x86_64 macOS wheels after 2.2.x, and the
#   last x86_64 macOS torch wheel is torch==2.12.1 built for cp39 (Python 3.9).
#   So the ONLY viable combo on this box is:  Python 3.9 (x86_64) + torch 2.2.2.
#
# Setup (from tools/visual-confirmation/):
#   python3.9 -m venv .venv
#   .venv/bin/python -m pip install --upgrade pip setuptools wheel
#   .venv/bin/python -m pip install -r requirements.txt
#
# torch's macOS x86_64 wheel is inherently CPU-only (no CUDA on macOS), which
# satisfies the "CPU-only torch" disk constraint without a special index.
#
# On a machine with a native arm64 Python, you'd instead use a current Python
# (3.11/3.12) + current torch arm64 wheel; coremltools/onnx/etc. all have arm64
# wheels. The conversion code is unchanged — only the interpreter/torch pins move.

torch==2.12.1
torchvision==0.17.2
coremltools==9.0
onnx==1.21.0
onnxruntime==1.19.2
opencv-python==4.10.0.84
numpy==1.26.4          # MUST be <2.0: torch 2.2.2 was built against numpy 1.x
pixeltable-yolox==0.4.2

# pixeltable-yolox pulls these transitively for its package __init__ import
# chain (training/eval utils) even though we only do inference + export:
loguru>=0.7.2
tabulate>=0.9.0
psutil>=5.9.7
pycocotools>=2.0.7
# (tensorboard / thop / onnxsim / ninja are training-only and intentionally
#  NOT installed — the import path we use does not need them.)
