// Tailspot.secrets.example.xcconfig — TEMPLATE only. Copy to
// Tailspot.secrets.xcconfig (gitignored) and paste the real value.
//
// SECURITY:
//   - The real Tailspot.secrets.xcconfig MUST NOT be committed.
//     `.gitignore` excludes it; verify before each `git add`.
//   - The PostHog key below is baked into Info.plist and is present in
//     the shipped binary. That's acceptable for an anonymous-analytics
//     write-only key. (OpenSky OAuth credentials used to live here too —
//     removed in the 2026-06-21 cutover; the app ships no extractable
//     API secret beyond this analytics key.)

// PostHog project API key for anonymous product analytics.
// Create a project at https://us.posthog.com, copy the Project API Key
// from Project Settings → Project API Key, and paste it below.
// When absent or empty, Analytics is a no-op (zero network activity).
POSTHOG_API_KEY = paste-your-posthog-api-key-here
